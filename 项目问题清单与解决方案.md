# 个人知识库项目问题清单与解决方案

更新时间：2026-04-11

## 1. 结论摘要

当前项目已具备可用的 MVP（文档上传、向量检索、问答、历史记录），但存在架构分叉、数据一致性和工程化不足等问题。建议按 P0 -> P1 -> P2 分阶段治理。

- P0：先解决架构重复和数据残留问题，避免后续维护成本持续上升
- P1：提升检索质量与系统稳定性
- P2：完善工程化与可观测性，支持持续迭代

---

## 2. 问题清单（按优先级）

## P0（必须优先）

### P0-1：RAG 实现双轨并存，逻辑重复
- 现象：chains/rag_chain.py 和 api/chat.py 各有一套问答拼装流程，实际请求路径主要走 chat.py。
- 影响：
  - 修改 Prompt、检索参数或模型配置时，容易漏改
  - 线上行为与预期文档不一致
- 根因：早期快速迭代时新增逻辑未完成收敛。
- 解决方案：
  1. 统一唯一入口：将聊天接口改为调用 chains/rag_chain.py 暴露的标准执行方法
  2. 将 prompt 模板、检索 top_k、温度、历史策略全部收敛到 chain 层配置
  3. 删除 chat.py 中重复的 prompt 拼接逻辑
- 验收标准：
  - 同一问题在同样参数下，chain 直调与 API 调用结果一致
  - 代码中仅保留一套可维护的 RAG 组装路径

### P0-2：删除文档仅删除向量，未删除原始文件
- 现象：删除接口删除 Chroma 中 chunks，但 data/documents 中源文件仍保留。
- 影响：
  - 磁盘占用持续增长
  - 数据治理不闭环，难以审计
- 根因：没有建立 file_id -> 物理文件路径的稳定映射与删除事务。
- 解决方案：
  1. 上传时记录文档索引（建议 JSON 或轻量 SQLite）
  2. 删除时执行两阶段：先删向量，再删文件；失败时记录补偿任务
  3. 增加后台清理任务，清除孤儿文件
- 验收标准：
  - 删除后向量和原文件均不存在
  - 系统可识别并清理历史残留孤儿文件

### P0-3：会话历史无限增长，缺少 token 控制
- 现象：每次请求都附加完整历史消息，缺少窗口、摘要或截断策略。
- 影响：
  - 响应时延和调用成本上升
  - 长对话下模型输出稳定性下降
- 根因：记忆功能只实现“存”，未实现“压缩与裁剪”。
- 解决方案：
  1. 增加 sliding window（例如最近 N 轮）
  2. 超阈值后生成摘要记忆（summary memory）
  3. 采用 token 预算策略，按预算动态裁剪历史
- 验收标准：
  - 100 轮对话后接口延迟可控
  - 历史长度在预算内，回答质量无明显退化

---

## P1（应尽快解决）

### P1-1：配置项未完全生效，配置与实现脱节
- 现象：config.py 中存在 EMBEDDING_MODEL，但向量层硬编码模型名。
- 影响：
  - 改模型需改代码，不利于环境切换
  - 配置文件权威性下降
- 根因：配置治理未贯穿到所有核心模块。
- 解决方案：
  1. 去除硬编码，统一读取 settings
  2. 启动时打印关键配置摘要（脱敏）
  3. 增加启动校验：API Key、目录权限、模型可用性
- 验收标准：
  - 改 .env 不改代码即可切换 Embedding 模型
  - 启动阶段能提前发现关键配置错误

### P1-2：文档索引来源于向量元数据反推，不是独立主数据
- 现象：文档列表接口通过遍历向量元数据聚合得到文档信息。
- 影响：
  - 向量异常时列表不可用
  - 文档级状态（上传时间、大小、状态）难扩展
- 根因：缺少文档主表（Document Registry）。
- 解决方案：
  1. 新建 document_registry（JSON/SQLite 均可）
  2. 上传、删除、重建索引都以 registry 为准
  3. list 接口仅查 registry，向量库只负责检索
- 验收标准：
  - 向量库重建期间，文档列表仍可正常展示
  - 文档元数据字段可稳定扩展

### P1-3：依赖与加载器潜在不匹配
- 现象：使用 UnstructuredWordDocumentLoader，但依赖清单未显式覆盖其运行所需依赖链。
- 影响：
  - 某些环境下 docx 解析失败
  - 部署与本地行为不一致
- 根因：requirements 只覆盖基础包，未锁定解析链关键依赖版本。
- 解决方案：
  1. 明确文档类型依赖矩阵并写入 requirements
  2. 增加健康检查接口（按类型跑最小解析测试）
  3. 引入最小可复现样本做集成测试
- 验收标准：
  - PDF、DOCX、MD、TXT 四类文件均可稳定解析
  - 新环境一键安装后无需额外手工补包

### P1-4：检索策略单一，召回质量上限受限
- 现象：当前主要为向量检索 + 直接拼接上下文。
- 影响：
  - 多意图、长问题、专业问题时误召回较多
- 根因：缺少重排和过滤机制。
- 解决方案：
  1. 支持 metadata 过滤（如 file_id、标签、时间）
  2. 增加 MMR 或 rerank
  3. 统一检索参数（top_k、score_threshold）并可配置化
- 验收标准：
  - 关键业务问题的命中率显著提升
  - 无关片段引用比例下降

---

## P2（工程化与持续优化）

### P2-1：缺少自动化测试与回归保障
- 现象：仓库无 tests 目录与测试基线。
- 影响：
  - 需求迭代后容易引入回归
  - 重构风险高
- 解决方案：
  1. 补齐三层测试：单元测试、API 测试、最小端到端
  2. 对上传、删除、检索、对话建立冒烟集
  3. CI 中加入测试与静态检查
- 验收标准：
  - 主流程具备自动化回归能力
  - 每次变更有可视化测试结果

### P2-2：日志与可观测性不足
- 现象：当前异常处理存在，但缺少结构化日志与核心指标。
- 影响：
  - 线上定位慢
  - 无法量化优化收益
- 解决方案：
  1. 引入结构化日志（请求 ID、会话 ID、耗时、召回条数）
  2. 记录关键指标：检索耗时、LLM 耗时、错误率、平均 token
  3. 错误分层：用户错误、依赖错误、系统错误
- 验收标准：
  - 任何一次失败请求可快速追踪到根因
  - 优化前后可用指标对比

### P2-3：安全与资源控制策略缺失
- 现象：CORS 全开放，上传文件未见大小和内容风险控制。
- 影响：
  - 生产暴露面较大
  - 可能出现资源滥用
- 解决方案：
  1. 按环境配置 CORS 白名单
  2. 增加上传大小限制、类型校验和内容扫描
  3. 增加请求限流与并发保护
- 验收标准：
  - 非白名单来源无法跨域访问
  - 超限上传被明确拦截并返回可读错误

---

## 3. 分阶段落地计划

### 第一阶段（1-2 天）
- 收敛 RAG 入口（P0-1）
- 完成删除闭环（P0-2）
- 增加历史裁剪策略（P0-3）

### 第二阶段（2-4 天）
- 配置治理与启动校验（P1-1）
- 建立 document_registry（P1-2）
- 补齐依赖与解析验证（P1-3）

### 第三阶段（3-5 天）
- 检索优化（P1-4）
- 自动化测试与 CI（P2-1）
- 日志指标与安全控制（P2-2/P2-3）

---

## 4. 建议的技术改造顺序（最小风险）

1. 先统一 chain 与 chat 的执行路径（低风险、高收益）
2. 再引入 document_registry，建立文档主数据
3. 然后补会话记忆裁剪与检索质量策略
4. 最后推进测试、日志、限流等工程化能力

---

## 5. 立即可执行的 10 条动作

1. 将 embedding 模型名改为统一读取 settings.EMBEDDING_MODEL
2. chat 接口改为调用统一 chain 服务层
3. 提取 Prompt 到独立模板函数，避免重复拼接
4. 给检索器增加 top_k 与 score_threshold 配置
5. 上传后记录文档索引（file_id、原始文件名、路径、chunk_count、ctime）
6. 删除接口同时删除向量和物理文件
7. 增加清理脚本：扫描并删除孤儿文件
8. 对会话历史加入最近 N 轮窗口
9. 补充最小测试样本（pdf/docx/md/txt 各 1 个）
10. 增加结构化日志字段（request_id、session_id、latency_ms）

---

如果你希望，我可以基于这份清单直接进入下一步：按 P0 顺序在当前代码里逐项落地改造。

---

## 6. 升级过程与亮点记录（2026-04-10）

### 6.1 已完成项

1. P0-1（RAG 入口收敛）
- 聊天接口统一调用 chain 层标准方法，减少重复拼装逻辑。

2. P0-2（删除闭环）
- 删除接口从“仅删向量”升级为“删向量 + 删本地文件（file_id.*）”，数据治理闭环。

3. P1-1（配置治理）
- Embedding 模型由硬编码改为统一读取 settings.EMBEDDING_MODEL，后续切换模型无需改代码。

4. P0-3（会话历史裁剪，最小可落地版）
- 增加历史消息窗口：MAX_HISTORY_MESSAGES_FOR_LLM（默认 12 条消息）。
- 增加字符预算：MAX_HISTORY_CHARS_FOR_LLM（默认 4000 字符）。
- 链路执行时统一先裁剪再调用 LLM，避免历史无限增长带来的时延和成本上升。

### 6.2 本轮升级亮点

1. 低入侵改造
- 保持 API 结构和调用方式不变，主要在 chain 层收口策略，风险可控。

2. 可配置可演进
- 历史窗口与预算通过配置控制，后续可按模型上下文大小和成本策略动态调整。

3. 可验证性增强
- 删除与会话策略都有明确验收目标：
  - 删除后向量和本地文件都不存在。
  - 长会话下请求体大小受控，不再无限增长。

### 6.3 下一步建议（按风险最小）

1. 给 P0-2 / P0-3 各补 1 条最小自动化测试，防回归。
2. 在 P0-3 基础上补 summary memory，进一步提升超长会话稳定性。

---

## 7. 实现对齐快照（2026-04-11）

### 7.1 本次新增完成项

1. 对话记忆编排切换为 LangChain 标准模式
- `RunnableWithMessageHistory` 已接入，对话历史由框架自动注入和回写。
- 历史存储继续使用 `FileChatMessageHistory`（`BaseChatMessageHistory` 实现），保持原有会话文件兼容。

2. API 返回结构保持不变
- `POST /api/chat/send` 仍返回 `answer + sources`，前端无需调整接口。

### 7.2 现状总览（已完成 / 待完成）

1. 已完成
- P0-1：RAG 执行路径收敛，且已采用 `RunnableWithMessageHistory`。
- P0-2：删除闭环（向量 + 本地文件）。
- P0-3：历史窗口和字符预算裁剪。
- P1-1：Embedding 模型配置化。

2. 待完成（优先）
- P1-2：引入 document_registry，解耦文档主数据与向量库。
- P1-3：补齐文档解析依赖矩阵与最小健康检查。
- P1-4：检索策略增强（top_k/score_threshold 配置化、MMR 或 rerank）。

### 7.3 建议下一步（可直接执行）

1. 先补 2 条最小回归测试：
- 对话历史自动回写（验证 `RunnableWithMessageHistory` 生效）。
- 删除接口硬删除一致性（向量与文件都删除）。
2. 然后推进 document_registry，降低后续功能扩展成本。