# RAG 升级计划表（渐进式）

## 1. 目标与原则
- 目标：在不推翻现有工程的前提下，逐步提升检索质量、回答稳定性、可观测性与生产可用性。
- 原则：小步快跑、每阶段可回滚、每阶段都有量化验收指标。
- 当前状态：工程已清理，准备先重建一个可运行的基础版 RAG（MVP），再按阶段升级。
- 学习目标：每个阶段都明确“改了什么、为什么改、效果提升了多少”，形成自己的 RAG 升级方法论。

## 2. 总体时间线（建议 10 周）
| 阶段 | 周期 | 主题 | 结果产物 |
|---|---:|---|---|
| Bootstrap | 第 0 周 | 基础版搭建 | 可运行 FastAPI + 本地知识库 + 最小评测 |
| Phase 0 | 第 1 周 | 基线与评测集 | 离线评测脚本 + 小型问答集 |
| Phase 1 | 第 2-3 周 | 文档处理升级 | 分块策略、清洗、元数据体系 |
| Phase 2 | 第 4-5 周 | 检索能力升级 | 混合检索 + 重排 + 查询改写 |
| Phase 3 | 第 6-7 周 | 生成与对话增强 | 上下文压缩、引用、答案约束 |
| Phase 4 | 第 8-9 周 | 工程化与稳定性 | 缓存、监控、限流、错误兜底 |
| Phase 5 | 第 10 周 | 验收与发布 | 回归报告 + 发布清单 |

## 3. 分阶段详细安排

## Bootstrap：基础版 RAG（第 0 周，先做这个）
### 目标
先有一个稳定、可反复演示、可测量的基础版，为后续升级打地基。

### 最小功能清单（MVP）
- 文档接入：支持读取本地 txt/md/pdf（先从 txt/md 开始）。
- 文档分块：先用固定分块（如 chunk_size=500, overlap=100）。
- 向量化：选择 1 个中文 embedding 模型（建议 bge-small-zh 或 bge-base-zh）。
- 向量库：先用 Chroma 持久化。
- 检索：向量相似度 Top-k（先 k=3）。
- 生成：LLM 根据检索上下文回答，提示词要求“未命中就说不知道”。
- 会话记忆：按 session_id 持久化（文件或 sqlite）。
- 入口：FastAPI 必选（至少包含 `/health` 与 `/chat`）；CLI 可选。

### 目录建议（基础版）
- app/config.py
- app/ingest.py
- app/retrieval.py
- app/generation.py
- app/history.py
- app/pipeline.py
- app/main.py
- app/api.py
- app/cli.py（可选）
- data/docs/
- data/chroma/
- eval/qa_set_small.json
- eval/run_eval.py

### 验收标准
- 能完成一轮“导入文档 -> 提问 -> 返回答案 -> 落盘历史”。
- 至少准备 20 条问答样本并能批量评测。
- 输出第一份基线报告（命中率、时延、幻觉样例）。

### Bootstrap 冻结范围（初始化期间不做）
- 不引入混合检索、重排、多查询改写。
- 不迁移 Milvus，先用 Chroma 跑通基线。
- 不处理复杂 OCR/表格，先支持 md/txt（pdf 放到后续）。
- 每次只改一个变量，确保收益可解释。

## Phase 0：基线与评测集（第 1 周）
### 目标
建立可量化基线，避免“感觉变好了”。

### 任务
- 建立 50-100 条领域问答评测集（含标准答案或关键点）。
- 定义指标：
  - 检索命中率（Top-1/Top-3）
  - 回答正确率（人工或半自动打分）
  - 平均响应时延（P50/P95）
  - 幻觉率（无依据回答占比）
- 固化评测脚本：同一输入可重复运行并输出对比报表。

### 验收标准
- 能输出一份基线报告（CSV 或 Markdown）。
- 任意一次改动都可复测并对比。

## Phase 1：文档处理升级（第 2-3 周）
### 目标
提高“被检索到”的概率，先解决召回问题。

### 任务
- 分块优化：
  - 试验 chunk_size（如 300/500/800）
  - 试验 overlap（如 50/100）
- 文档清洗：去噪、去模板文本、统一编码。
- 元数据规范：source、title、section、created_at、tags。
- 建立增量索引与硬删除一致性流程（已确认需删除向量 + 本地文件）。

### 验收标准
- Top-3 命中率较基线提升 >= 10%。
- 重复文档和脏文本显著下降（可抽样检查）。

## Phase 2：检索能力升级（第 4-5 周）
### 目标
从“单向量检索”升级为“混合检索 + 重排”。

### 任务
- 引入混合检索：BM25（关键词）+ 向量检索融合。
- 增加重排模型（Cross-Encoder 或 API 重排）。
- 查询改写：
  - 拼写纠错
  - 同义词扩展
  - 多查询扩展（Multi-Query）
- 检索失败兜底：低置信度时触发“澄清提问”。

### 验收标准
- Top-1 命中率较 Phase 1 再提升 >= 8%。
- 幻觉率下降 >= 20%。

### 学习重点（你要掌握的检索升级点）
- 为什么 Hybrid Search 往往比单向量检索更稳。
- 为什么 Rerank 对 Top-k 结果质量提升明显。
- Multi-Query 和 Query Rewrite 分别解决什么问题。
- 什么时候该调 chunk，什么时候该换 embedding。

## Phase 3：生成与对话增强（第 6-7 周）
### 目标
提升回答可靠性与可解释性。

### 任务
- 上下文压缩：减少无关片段，控制 token 成本。
- 回答模板升级：
  - 强制“依据-结论”结构
  - 输出引用来源（source/title/段落）
- 会话记忆增强：
  - 保留长期偏好
  - 会话摘要压缩旧历史
- 拒答策略：无依据时明确说明“知识库未命中”。

### 验收标准
- 回答中可追溯引用覆盖率 >= 90%。
- 平均 token 成本下降 >= 15%（相同问题集）。

## Phase 4：工程化与稳定性（第 8-9 周）
### 目标
从“可用”走向“稳定可持续”。

### 任务
- 缓存：
  - 查询缓存（短 TTL）
  - 检索结果缓存
- 监控与日志：
  - 请求链路日志（query, top_k, latency）
  - 错误分类告警
- 限流与超时：防止高并发拖垮。
- 可靠性：
  - API Key 与配置环境变量化
  - 异常降级（LLM 超时时返回保底答案）

### 验收标准
- P95 响应时延下降 >= 25%。
- 错误率可观测，且能定位到模块级别。

## Phase 5：验收与发布（第 10 周）
### 目标
形成可复用版本与发布规范。

### 任务
- 完整回归测试（功能 + 评测集 + 压测）。
- 输出发布清单：版本号、变更点、回滚方案。
- 编写运维手册：常见故障与排查步骤。

### 验收标准
- 关键指标达到目标阈值。
- 具备可回滚发布流程。

## 4. 指标看板（建议）
| 指标 | 当前基线 | 目标值 | 统计方式 |
|---|---:|---:|---|
| Top-1 命中率 | 待测 | +20%（总） | 离线评测集 |
| Top-3 命中率 | 待测 | +30%（总） | 离线评测集 |
| 幻觉率 | 待测 | -30% | 人工抽样 + 规则 |
| P95 时延 | 待测 | -25% | 服务日志 |
| 平均 token 成本 | 待测 | -20% | 调用统计 |

## 5. 每周执行模板（可复制）
## Week X
- 本周目标：
- 本周改动：
- 评测结果（对比上周）：
- 风险与阻塞：
- 下周计划：

## 6. 当前代码的优先改造顺序（从你现状出发）
1. 先完成 Bootstrap 基础版（可运行、可评测）。
2. 再做评测集与基线脚本（Phase 0）。
3. 把文档分块和元数据补齐（Phase 1）。
4. 上混合检索与重排（Phase 2）。
5. 再做回答引用和拒答策略（Phase 3）。
6. 最后补缓存、监控和发布流程（Phase 4-5）。

## 7. 风险清单与规避
- 风险：只改 Prompt 不做检索侧改造，收益有限。
  - 规避：优先投入检索与重排。
- 风险：没有评测集导致优化方向漂移。
  - 规避：每次改动必须跑评测。
- 风险：上下文过长导致成本和时延上升。
  - 规避：做上下文压缩与 top_k 动态控制。

## 8. 里程碑打勾清单
- [ ] B0：完成基础版 RAG（Bootstrap）
- [ ] M1：完成基线评测
- [ ] M2：完成分块与元数据优化
- [ ] M3：完成混合检索 + 重排
- [ ] M4：完成引用与拒答机制
- [ ] M5：完成监控与发布验收

## 9. 向量检索升级选型表（重点）
## 9.1 Embedding 模型可以改成什么
| 方案 | 适用场景 | 优点 | 代价/风险 |
|---|---|---|---|
| BAAI/bge-small-zh-v1.5 | 入门与本地调试 | 速度快、资源占用低 | 上限一般 |
| BAAI/bge-base-zh-v1.5 | 中文通用检索 | 质量更稳 | 速度慢于 small |
| bge-m3 | 多语言/多功能检索 | 综合能力强 | 资源开销更高 |
| jina-embeddings 系列 | 英文或多语言 | 社区使用广 | 中文需实测 |
| API Embedding（商业） | 线上质量优先 | 无需本地推理 | 成本与网络依赖 |

## 9.2 向量库可以改成什么
| 方案 | 适用场景 | 优点 | 代价/风险 |
|---|---|---|---|
| Chroma | 本地学习、快速原型 | 简单易用 | 大规模能力一般 |
| FAISS | 纯本地高性能检索 | 检索快、可控性高 | 工程封装要自己做 |
| Milvus | 中大型生产检索 | 扩展性好 | 部署复杂 |
| Elasticsearch/OpenSearch 向量 | 已有 ES 体系 | 与关键词检索融合方便 | 资源开销较大 |

## 9.3 检索策略可以改成什么
| 策略 | 何时使用 | 收益 |
|---|---|---|
| Dense Retrieval（单向量） | 基础版 | 快速上线 |
| BM25 | 关键词强约束问题 | 精确词命中提升 |
| Hybrid（BM25 + Dense） | 大多数知识库 | 召回更稳 |
| Rerank（Cross-Encoder/API） | Top-k 有噪声 | Top-1/Top-3 提升明显 |
| Multi-Query | 用户表达模糊 | 召回覆盖提升 |
| Query Rewrite | 用户问题过短/含糊 | 检索相关性提升 |

## 10. 迭代实验方法（防止“改了但不知道为何变好”）
- 每次只改一个变量：例如只改 chunk_size 或只加 rerank。
- 每次改动都保留实验记录：
  - 配置：模型、k 值、chunk 参数。
  - 指标：Top-1、Top-3、时延、幻觉率。
  - 结论：是否进入主分支，为什么。
- 统一实验命名：`exp_日期_改动点`，例如 `exp_2026-04-15_add_rerank`。

## 11. 你可以直接开工的第一周清单
1. 重建基础目录与最小运行链路（Bootstrap）。
2. 准备 20 条小评测集并写 `run_eval.py`。
3. 跑出第一份基线报告（Markdown 即可）。
4. 先做一次对照实验：`bge-small-zh` vs `bge-base-zh`。
5. 总结一页实验结论，作为 Phase 1 输入。

## 12. 第二周强化模块（可选并推荐）
你提供的第二周学习内容整体方向很好，建议加入计划，但采用“分层启用”，避免一次性堆太多技术导致难以定位收益。

### 12.1 建议纳入的内容（高优先级）
1. Query Transformation（HyDE + Multi-Query）
- 价值：提升召回覆盖率，尤其对口语化、模糊问题效果明显。
- 验收：Top-3 命中率提升，且平均延迟可控。

2. Hybrid Retrieval + Rerank
- 价值：通常是最稳定的质量增益组合。
- 建议顺序：先 Hybrid，再 Rerank，再调参数。
- 验收：Top-1 明显提升，幻觉率下降。

3. RAG 自动评测（RAGAs 或 DeepEval）
- 价值：从“凭感觉优化”切换到“数据驱动优化”。
- 验收：可生成对比报告，至少覆盖忠实度与相关性。

### 12.2 建议暂缓或按需纳入（中优先级）
1. Milvus（生产级向量库）
- 建议在以下条件满足后再上：
  - 已完成基础版并有评测基线。
  - Chroma 在数据量或并发上出现瓶颈。
- 否则先用 Chroma/FAISS，把精力放在检索策略更划算。

2. 高级文档解析（Unstructured、MinerU、Docling）
- 适合文档主要是 PDF、扫描件、表格图片时再引入。
- 若当前资料主要是 md/txt，可暂缓，先把检索链条跑顺。

### 12.3 第二周建议日程（精简版）
1. Day 8：HyDE + Multi-Query 小实验。
2. Day 9：Hybrid 检索落地。
3. Day 10：接入 Rerank 并做参数对比。
4. Day 11：接入 RAGAs/DeepEval 输出自动评测报告。
5. Day 12：评估是否需要 Milvus（满足条件再迁移）。
6. Day 13：按数据类型决定是否接入复杂文档解析。
7. Day 14：整理周报与实验复盘，冻结一个“第二周稳定版本”。

### 12.4 我给你的取舍原则（比“全上”更重要）
- 原则 1：每次只上线一个变量，否则无法知道收益来自哪里。
- 原则 2：优先做“检索侧”优化，再做“生成侧”修饰。
- 原则 3：只有当现有组件成为瓶颈时，才迁移基础设施（如向量库）。
- 原则 4：先拿到稳定提升，再追求复杂度。
  
## 13. 初始化交付清单（与你当前项目对齐）
1. 交付一个可运行的 Naive RAG 骨架（FastAPI 必选，CLI 可选）。
2. 支持 md/txt 导入、分块、向量化、检索问答、历史落盘。
3. 提供 20 条小评测集与基础评测脚本。
4. 代码结构预留检索升级接口（hybrid/rerank/query transform）。
5. 形成 `B0` 版本快照，作为后续升级对照基线。
